package com.wellpaid.navigation

/**
 * [androidx.navigation.NavBackStackEntry.savedStateHandle] key: request [MainShellScreen] to select a tab index (0..4).
 * Written before popping back to Main (e.g. shopping lists swipe-to-home).
 */
const val MAIN_SHELL_SELECT_TAB = "main_shell_select_tab"
